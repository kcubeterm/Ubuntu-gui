name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: login to heroku
      env:
         heroku_api_key: ${{secrets.HEROKU_API_KEY}}

         run: echo "ubuntu-gui" | bash installnovnc
